{
  "name": "delegate-to-local",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "delegate": "scripts/delegate-loop.ts",
    "typecheck": "tsc --noEmit",
    "test": "test/run.sh"
  },
  "devDependencies": {
    "@types/bun": "^1.4.0",
    "typescript": "5"
  }
}
